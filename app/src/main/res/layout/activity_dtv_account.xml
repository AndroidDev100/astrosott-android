<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        tools:context="com.dialog.dialoggo.activities.subscription.ui.DtvAccountActivity">

        <fragment
            android:id="@+id/containerFragmentAAA"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="@dimen/fragment_width"
            android:layout_height="@dimen/fragment_height"
            android:fillViewport="false"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_dtv_dialog_graph" />

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:theme="@style/progressBarBlue"
            android:visibility="gone" />
    </RelativeLayout>
</layout>